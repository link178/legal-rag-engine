"""Citation extraction / verification utilities (placeholder)."""

from typing import Any


def verify_citations(answer: str, contexts: list[Any]) -> list[Any]:
    """Align answer spans with source chunks (not implemented)."""
    raise NotImplementedError
