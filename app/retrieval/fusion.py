"""Fusion of dense and sparse result lists (placeholder)."""

from typing import Any


def fuse_results(
    dense_hits: list[Any],
    sparse_hits: list[Any],
) -> list[Any]:
    """Merge and rank combined hits (not implemented)."""
    raise NotImplementedError
